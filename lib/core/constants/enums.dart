enum UserRole {
  tenant,
  landowner,
  manager,
  artisan
}

enum PriorityLevel {
  low,
  medium,
  high,
  emergency
}

enum PaymentStatus {
  pending,
  paid,
  overdue,
  refunded
}

enum TaskStatus {
  pending,
  assigned,
  inProgress,
  completed,
  approved,
  rejected
}

enum PropertyType {
  apartment,
  house,
  commercial
}

enum AmenityType {
  wifi,
  parking,
  pool,
  gym,
  balcony,
  furnished,
  petFriendly,
  airConditioning
}